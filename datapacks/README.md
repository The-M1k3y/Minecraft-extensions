# Data Packs

Server-side Minecraft data packs used by this project.

Create one subdirectory per independently deployable data pack. Keep pack-specific documentation close to the implementation, while behavior and compatibility decisions that span multiple components belong in `../docs/`.

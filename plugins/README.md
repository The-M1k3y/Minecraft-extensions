# Paper Plugins

Paper plugins used by this project.

Create one subdirectory per independently deployable plugin. Prefer plugins only for behavior that cannot be expressed cleanly with vanilla data packs/resource packs, or where Paper event/registry APIs are required for compatibility and correctness.

Shared behavioral requirements belong in `../docs/`.
